import { useMemo, useState } from "react";
import { AlertTriangle, ArrowLeft, BarChart3, CheckCircle2, Clock3, Filter, MapPin, ShieldCheck, Siren, UserRound, Wrench, ChevronRight, Radio, Zap } from "lucide-react";
import { MapContainer, Popup, TileLayer, CircleMarker, useMap } from "react-leaflet";
import L from "leaflet";
import { confirmedHazards } from "../sampleData";
import { getLocalReports } from "../firebase";

const JAIPUR_CENTER = [26.9124, 75.7873];
const ACTION_KEY = "roadsense_authority_actions";
const severityRank = { critical: 4, high: 3, medium: 2, low: 1 };
const severityColor = { critical: "#dc2626", high: "#f97316", medium: "#eab308", low: "#16a34a" };
const SLA_HOURS = { critical: 4, high: 12, medium: 24, low: 48 };

function readActions() { try { return JSON.parse(localStorage.getItem(ACTION_KEY)) || {}; } catch { return {}; } }
function ageHours(createdAt) { if (!createdAt) return 0; return Math.max(0, (Date.now() - new Date(createdAt).getTime()) / 3600000); }
function formatAge(createdAt) { const hours = Math.floor(ageHours(createdAt)); if (hours < 1) return "<1h"; if (hours < 24) return `${hours}h`; return `${Math.floor(hours / 24)}d`; }
function getDepartment(title = "") { const text = title.toLowerCase(); if (text.includes("water") || text.includes("flood") || text.includes("drain")) return "Drainage / Water"; if (text.includes("traffic") || text.includes("signal")) return "Traffic Engineering"; if (text.includes("accident") || text.includes("collision")) return "Traffic Police"; return "Road Maintenance"; }
function MapCenter({ points }) { const map = useMap(); if (points.length) { const bounds = L.latLngBounds(points.map((point) => [point.coordinates.lat, point.coordinates.lng])); if (bounds.isValid()) map.fitBounds(bounds.pad(0.18), { animate: false }); } return null; }

export default function AuthorityDashboard({ onBack, onNavigate }) {
  const [actions, setActions] = useState(readActions);
  const [filter, setFilter] = useState("all");
  const [selectedId, setSelectedId] = useState(null);
  const [view, setView] = useState("actions");

  const reports = useMemo(() => getLocalReports().map((report) => {
    const saved = actions[report.id] || {};
    const title = report.hazardType || "Road hazard";
    const severity = saved.severity || (report.upvotes >= 3 ? "high" : "medium");
    return { ...report, id: report.id, title, severity, status: saved.status || (report.status === "verified" ? "assigned" : "new"), assignedTo: saved.assignedTo || getDepartment(title), createdAt: report.createdAt, age: formatAge(report.createdAt), ageInHours: ageHours(report.createdAt), source: "Citizen report" };
  }), [actions]);

  const incidents = useMemo(() => {
    const seeded = confirmedHazards.map((hazard) => ({ id: `hazard_${hazard.id}`, title: hazard.type || "Verified hazard", location: hazard.location, coordinates: hazard.coordinates, severity: hazard.severity === "red" ? "critical" : hazard.severity === "orange" ? "high" : "medium", status: "verified", assignedTo: "Road Safety Cell", source: hazard.source || "Verified hazard", age: "Verified", ageInHours: 0 }));
    return [...reports, ...seeded];
  }, [reports]);

  const actionable = incidents.filter((item) => item.status !== "verified");
  const visible = incidents.filter((item) => filter === "all" || item.severity === filter || item.status === filter);
  const critical = incidents.filter((item) => item.severity === "critical" && item.status !== "verified").length;
  const open = actionable.filter((item) => item.status !== "resolved").length;
  const resolved = incidents.filter((item) => ["resolved", "verified"].includes(item.status)).length;
  const overdue = actionable.filter((item) => item.ageInHours >= SLA_HOURS[item.severity] && item.status !== "resolved").length;
  const selected = incidents.find((item) => item.id === selectedId) || visible[0];
  const mapPoints = visible.filter((item) => item.coordinates?.lat && item.coordinates?.lng).slice(0, 80);

  const updateAction = (id, patch) => { const next = { ...actions, [id]: { ...(actions[id] || {}), ...patch } }; setActions(next); localStorage.setItem(ACTION_KEY, JSON.stringify(next)); };
  const advance = (item) => { const nextStatus = { new: "assigned", assigned: "in_progress", in_progress: "resolved", resolved: "verified" }[item.status] || "assigned"; updateAction(item.id, { status: nextStatus, assignedTo: item.assignedTo || getDepartment(item.title), updatedAt: new Date().toISOString() }); };
  const escalate = (item) => updateAction(item.id, { severity: "critical", escalated: true, status: item.status === "new" ? "assigned" : item.status, assignedTo: item.assignedTo || getDepartment(item.title) });
  const actionLabel = (status) => ({ new: "Assign", assigned: "Start", in_progress: "Resolve", resolved: "Verify" }[status] || "Manage");
  const slaState = (item) => { if (item.status === "verified") return "Closed"; const limit = SLA_HOURS[item.severity] || 24; if (item.ageInHours >= limit) return "Breached"; if (item.ageInHours >= limit * 0.75) return "At risk"; return "On track"; };

  return (
    <div className="authority-screen">
      <style>{`.authority-screen{min-height:100vh;background:var(--bg);color:var(--text)}.authority-screen .authority-content{max-width:1120px;margin:0 auto;padding:22px 22px 34px}.authority-header{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:16px 22px;border-bottom:1px solid var(--border);background:var(--surface);position:sticky;top:0;z-index:20;box-shadow:0 2px 12px rgba(15,23,42,.05)}.authority-brand{display:flex;align-items:center;gap:12px;min-width:0}.authority-brand .icon-btn{width:38px;height:38px;border:1px solid var(--border);border-radius:10px;background:var(--surface);color:var(--text);display:grid;place-items:center;cursor:pointer}.authority-brand .icon-btn:hover{background:var(--bg)}.authority-brand>div{display:flex;flex-direction:column;gap:2px}.authority-brand strong{font-size:14px;font-weight:800;letter-spacing:-.2px}.authority-brand span{font-size:10px;color:var(--muted)}.authority-user{display:flex;align-items:center;gap:7px;padding:8px 11px;border:1px solid #ddd6fe;border-radius:999px;background:#faf5ff;color:#6d28d9;font-size:10px;font-weight:750;white-space:nowrap}.authority-hero{display:flex;align-items:flex-end;justify-content:space-between;gap:20px;padding:8px 2px 20px}.authority-hero p{margin:0 0 5px;color:#6d28d9;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.1em}.authority-hero h1{margin:0;font-size:25px;line-height:1.15;letter-spacing:-.6px}.authority-hero span{display:block;margin-top:7px;color:var(--muted);font-size:11px;max-width:620px;line-height:1.5}.authority-date{display:flex;align-items:center;gap:6px;padding:7px 10px;border:1px solid var(--border);border-radius:8px;background:var(--surface);font-size:9px;color:var(--muted);white-space:nowrap}.authority-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:18px}.authority-kpi{display:grid;grid-template-columns:auto 1fr;grid-template-rows:auto auto;column-gap:10px;align-items:center;padding:14px;border:1px solid var(--border);border-radius:12px;background:var(--surface);box-shadow:0 2px 10px rgba(15,23,42,.04)}.authority-kpi .kpi-icon{grid-row:1/3;width:34px;height:34px;border-radius:9px;display:grid;place-items:center}.kpi-icon.red{background:#fee2e2;color:#dc2626}.kpi-icon.orange{background:#ffedd5;color:#ea580c}.kpi-icon.green{background:#dcfce7;color:#16a34a}.kpi-icon.yellow{background:#fef3c7;color:#ca8a04}.authority-kpi strong{font-size:19px;line-height:1}.authority-kpi small{font-size:9px;color:var(--muted);margin-top:3px}.authority-tabs{display:flex;gap:4px;margin:0 0 14px;padding:4px;background:var(--surface);border:1px solid var(--border);border-radius:11px;width:max-content;box-shadow:0 2px 8px rgba(15,23,42,.03)}.authority-tabs button{display:flex;align-items:center;gap:6px;border:0;background:transparent;color:var(--muted);padding:9px 12px;border-radius:8px;font-size:10px;font-weight:750;cursor:pointer;transition:.15s}.authority-tabs button:hover{color:var(--text);background:var(--bg)}.authority-tabs button.active{background:#6d28d9;color:#fff;box-shadow:0 2px 7px rgba(109,40,217,.22)}.authority-grid{display:grid;grid-template-columns:minmax(0,1.05fr) minmax(0,.95fr);gap:14px}.authority-panel{min-width:0;border:1px solid var(--border);border-radius:13px;background:var(--surface);overflow:hidden;box-shadow:0 3px 14px rgba(15,23,42,.045)}.authority-panel-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;padding:14px 15px;border-bottom:1px solid var(--border)}.authority-panel-head h2{margin:0;font-size:13px;letter-spacing:-.15px}.authority-panel-head p{margin:4px 0 0;color:var(--muted);font-size:9px}.authority-filter{display:flex;align-items:center;gap:5px;border:1px solid var(--border);background:var(--surface);color:var(--muted);border-radius:7px;padding:6px 8px;font-size:8px;font-weight:700;cursor:pointer}.authority-map{height:310px;position:relative}.authority-map .leaflet-container{height:100%;width:100%;font-family:inherit}.map-legend{position:absolute;left:10px;bottom:10px;z-index:500;display:flex;gap:7px;padding:7px 9px;border:1px solid var(--border);border-radius:8px;background:rgba(255,255,255,.94);box-shadow:0 2px 8px rgba(0,0,0,.08);font-size:8px;color:#374151}.map-legend span{display:flex;align-items:center;gap:4px}.legend-dot{width:7px;height:7px;border-radius:50%;display:inline-block}.legend-dot.critical{background:#dc2626}.legend-dot.high{background:#f97316}.legend-dot.medium{background:#eab308}.filter-pills{display:flex;gap:5px;flex-wrap:wrap;padding:10px 12px;border-bottom:1px solid var(--border)}.filter-pills button{border:1px solid var(--border);background:var(--surface);color:var(--muted);border-radius:999px;padding:5px 8px;font-size:8px;font-weight:700;cursor:pointer}.filter-pills button.active{background:#ede9fe;color:#6d28d9;border-color:#c4b5fd}.authority-queue{max-height:345px;overflow:auto}.authority-incident{display:grid;grid-template-columns:5px minmax(0,1fr) auto;gap:10px;padding:12px;border-bottom:1px solid var(--border);cursor:pointer;transition:background .15s}.authority-incident:last-child{border-bottom:0}.authority-incident:hover,.authority-incident.selected{background:#fafafa}.incident-severity{width:4px;border-radius:4px;min-height:58px}.incident-main{min-width:0}.incident-main>strong{display:block;font-size:10px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.incident-main>span{display:flex;align-items:center;gap:4px;margin-top:4px;color:var(--muted);font-size:8px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.incident-main>small{display:block;margin-top:4px;color:var(--muted);font-size:7px;text-transform:capitalize}.incident-main em.sla-chip{display:inline-flex;font-style:normal;width:max-content;border-radius:999px;padding:3px 6px;font-size:7px;font-weight:800;margin-top:5px;background:#dcfce7;color:#15803d}.incident-main em.sla-chip.at-risk{background:#fef3c7;color:#a16207}.incident-main em.sla-chip.breached{background:#fee2e2;color:#b91c1c}.incident-main em.sla-chip.closed{background:#e0e7ff;color:#4338ca}.incident-actions{display:flex;flex-direction:column;justify-content:center;gap:5px}.incident-actions button,.detail-actions button,.escalate-btn{border:1px solid #c4b5fd;background:#faf5ff;color:#6d28d9;border-radius:7px;padding:6px 8px;font-size:8px;font-weight:800;cursor:pointer;white-space:nowrap}.incident-actions button:hover,.detail-actions button:hover{background:#ede9fe}.detail-actions button:last-child,.incident-actions .escalate-btn{background:var(--surface);color:var(--muted);border-color:var(--border)}.authority-detail{padding:18px}.detail-severity{font-size:9px;font-weight:900;letter-spacing:.1em}.authority-detail h2{font-size:17px;margin:6px 0 0}.authority-detail>p{font-size:10px;color:var(--muted);margin:5px 0 0}.detail-grid{display:grid;grid-template-columns:1fr 1fr;gap:1px;margin-top:16px;background:var(--border);border:1px solid var(--border);border-radius:9px;overflow:hidden}.detail-grid span{padding:10px;background:var(--surface);font-size:8px;color:var(--muted)}.detail-grid strong{display:block;color:var(--text);font-size:10px;margin-top:3px;text-transform:capitalize}.detail-actions{display:flex;gap:7px;margin-top:13px}.authority-empty{padding:28px 16px;text-align:center;color:var(--muted);font-size:9px}.analytics-grid{grid-template-columns:1.1fr .9fr}.performance-grid{display:grid;grid-template-columns:1fr 1fr;gap:1px;background:var(--border);border:1px solid var(--border);margin:14px;border-radius:9px;overflow:hidden}.performance-grid>div{padding:15px;background:var(--surface)}.performance-grid strong{display:block;font-size:18px}.performance-grid span{display:block;color:var(--muted);font-size:8px;margin-top:4px}.workload-list{padding:8px 14px 12px}.workload-row{padding:10px 0;border-bottom:1px solid var(--border)}.workload-row:last-child{border-bottom:0}.workload-row>div:first-child{display:flex;justify-content:space-between;gap:8px}.workload-row strong{font-size:9px}.workload-row span{font-size:8px;color:var(--muted)}.workload-track{height:5px;background:var(--bg);border-radius:999px;margin-top:7px;overflow:hidden}.workload-track i{display:block;height:100%;background:#8b5cf6;border-radius:999px}.authority-footer-panel{margin-top:14px;padding:12px 15px;display:flex;align-items:center;justify-content:space-between;gap:18px}.authority-footer-panel>div:first-child strong,.authority-footer-panel>div:first-child span{display:block}.authority-footer-panel>div:first-child strong{font-size:10px}.authority-footer-panel>div:first-child span{font-size:8px;color:var(--muted);margin-top:3px}.authority-note{display:flex;align-items:center;gap:7px;color:var(--muted);font-size:8px;line-height:1.4;max-width:560px}.authority-note svg{flex:none}@media(max-width:760px){.authority-screen .authority-content{padding:16px 12px 26px}.authority-header{padding:13px 12px}.authority-hero{align-items:flex-start;flex-direction:column;gap:10px}.authority-hero h1{font-size:21px}.authority-kpis{grid-template-columns:1fr 1fr}.authority-grid,.analytics-grid{grid-template-columns:1fr}.authority-tabs{width:100%;overflow:auto}.authority-tabs button{flex:1;justify-content:center;white-space:nowrap}.authority-map{height:270px}.authority-footer-panel{display:block}.authority-note{margin-top:10px;max-width:none}}@media(max-width:430px){.authority-user span{display:none}.authority-user{padding:8px}.authority-kpi{padding:11px}.authority-kpi strong{font-size:17px}.authority-incident{grid-template-columns:4px minmax(0,1fr) auto;gap:7px;padding:10px}.incident-actions button{padding:6px;font-size:7px}.authority-map{height:245px}}`}</style>
      <header className="authority-header"><div className="authority-brand"><button className="icon-btn" onClick={onBack || (() => onNavigate("home"))} aria-label="Back to RoadSense"><ArrowLeft size={20} /></button><div><strong>RoadSense Authority</strong><span>Road safety command centre</span></div></div><div className="authority-user"><ShieldCheck size={18} /><span>Officer mode</span></div></header>
      <main className="authority-content">
        <section className="authority-hero"><div><p>Live operations</p><h1>Authority Action Center</h1><span>Prioritize risk, assign teams, track SLA and close road-safety incidents.</span></div><div className="authority-date"><Radio size={12} /> Live · Jaipur</div></section>
        <section className="authority-kpis"><div className="authority-kpi"><span className="kpi-icon red"><Siren size={17} /></span><strong>{critical}</strong><small>Critical actions</small></div><div className="authority-kpi"><span className="kpi-icon orange"><Clock3 size={17} /></span><strong>{open}</strong><small>Open actions</small></div><div className="authority-kpi"><span className="kpi-icon green"><CheckCircle2 size={17} /></span><strong>{resolved}</strong><small>Resolved / verified</small></div><div className="authority-kpi"><span className="kpi-icon yellow"><AlertTriangle size={17} /></span><strong>{overdue}</strong><small>SLA breached</small></div></section>
        <div className="authority-tabs">{[{ key: "actions", label: "Action Center", icon: Zap }, { key: "map", label: "Live Map", icon: MapPin }, { key: "analytics", label: "Analytics", icon: BarChart3 }].map(({ key, label, icon: Icon }) => <button key={key} className={view === key ? "active" : ""} onClick={() => setView(key)}><Icon size={15} />{label}</button>)}</div>
        {view !== "analytics" && <section className="authority-grid">
          <div className="authority-panel authority-map-panel"><div className="authority-panel-head"><div><h2>{view === "map" ? "Live hazard command map" : "Hazard command map"}</h2><p>Citizen reports, verified hazards and risk hotspots</p></div><button className="authority-filter"><Filter size={15} /> Filters</button></div><div className="authority-map"><MapContainer center={JAIPUR_CENTER} zoom={11} scrollWheelZoom={false} zoomControl={false}><TileLayer attribution='&copy; OpenStreetMap contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" /><MapCenter points={mapPoints} />{mapPoints.map((item) => <CircleMarker key={item.id} center={[item.coordinates.lat, item.coordinates.lng]} radius={item.severity === "critical" ? 9 : 7} pathOptions={{ color: severityColor[item.severity] || "#6b7280", fillColor: severityColor[item.severity] || "#6b7280", fillOpacity: .82 }} eventHandlers={{ click: () => setSelectedId(item.id) }}><Popup><strong>{item.title}</strong><br />{item.location || "Citizen report"}<br /><small>{item.status} · {item.assignedTo}</small></Popup></CircleMarker>)}</MapContainer><div className="map-legend"><span><i className="legend-dot critical" />Critical</span><span><i className="legend-dot high" />High</span><span><i className="legend-dot medium" />Medium</span></div></div></div>
          {view === "actions" && <div className="authority-panel priority-panel"><div className="authority-panel-head"><div><h2>Priority action queue</h2><p>{actionable.length} incidents in operational workflow</p></div><BarChart3 size={19} /></div><div className="filter-pills">{["all", "critical", "high", "medium", "new", "assigned", "in_progress"].map((value) => <button key={value} className={filter === value ? "active" : ""} onClick={() => setFilter(value)}>{value === "all" ? "All" : value === "in_progress" ? "In progress" : value[0].toUpperCase() + value.slice(1)}</button>)}</div><div className="authority-queue">{visible.slice().sort((a, b) => severityRank[b.severity] - severityRank[a.severity] || b.ageInHours - a.ageInHours).slice(0, 8).map((item) => <article key={item.id} className={`authority-incident ${selectedId === item.id ? "selected" : ""}`} onClick={() => setSelectedId(item.id)}><span className="incident-severity" style={{ background: severityColor[item.severity] || "#6b7280" }} /><div className="incident-main"><strong>{item.title}</strong><span><MapPin size={12} />{item.location || "Citizen submitted report"}</span><small>{item.status} · {item.assignedTo} · {item.age || "recent"}</small><em className={`sla-chip ${slaState(item).toLowerCase().replace(" ", "-")}`}>{slaState(item)}</em></div><div className="incident-actions">{item.status !== "verified" && <button onClick={(event) => { event.stopPropagation(); advance(item); }}>{actionLabel(item.status)}</button>}{item.status !== "verified" && item.severity !== "critical" && <button className="escalate-btn" onClick={(event) => { event.stopPropagation(); escalate(item); }}>Escalate</button>}</div></article>)}{!visible.length && <div className="authority-empty">No incidents match this filter.</div>}</div></div>}
          {view === "map" && <div className="authority-panel"><div className="authority-panel-head"><div><h2>Selected incident</h2><p>Click any map marker to inspect and act</p></div><ChevronRight size={18} /></div>{selected ? <div className="authority-detail"><div className="detail-severity" style={{ color: severityColor[selected.severity] }}>{selected.severity.toUpperCase()}</div><h2>{selected.title}</h2><p>{selected.location || "Citizen submitted report"}</p><div className="detail-grid"><span>Status<strong>{selected.status}</strong></span><span>Owner<strong>{selected.assignedTo}</strong></span><span>Age<strong>{selected.age}</strong></span><span>SLA<strong>{slaState(selected)}</strong></span></div>{selected.status !== "verified" && <div className="detail-actions"><button onClick={() => advance(selected)}>{actionLabel(selected.status)}</button><button onClick={() => escalate(selected)}>Escalate</button></div>}</div> : <div className="authority-empty">Select a hazard marker.</div>}</div>}
        </section>}
        {view === "analytics" && <section className="authority-grid analytics-grid"><div className="authority-panel"><div className="authority-panel-head"><div><h2>Operational performance</h2><p>Derived from the current RoadSense action queue</p></div><BarChart3 size={18} /></div><div className="performance-grid"><div><strong>{incidents.length ? Math.round((resolved / incidents.length) * 100) : 0}%</strong><span>Resolved / verified</span></div><div><strong>{incidents.length ? Math.round((incidents.filter((i) => i.status !== "verified").reduce((sum, i) => sum + i.ageInHours, 0) / Math.max(1, actionable.length)) * 10) / 10 : 0}h</strong><span>Avg. open age</span></div><div><strong>{overdue}</strong><span>SLA breaches</span></div><div><strong>{new Set(incidents.map((i) => i.assignedTo)).size}</strong><span>Active owners</span></div></div></div><div className="authority-panel"><div className="authority-panel-head"><div><h2>Department workload</h2><p>Current assignment distribution</p></div><Wrench size={18} /></div><div className="workload-list">{["Road Maintenance", "Traffic Engineering", "Drainage / Water", "Traffic Police"].map((dept) => { const count = incidents.filter((i) => i.assignedTo === dept && i.status !== "verified").length; return <div className="workload-row" key={dept}><div><strong>{dept}</strong><span>{count} active cases</span></div><div className="workload-track"><i style={{ width: `${Math.min(100, count * 18)}%` }} /></div></div>; })}</div></div></section>}
        <section className="authority-panel authority-footer-panel"><div><strong>Workflow</strong><span>Receive → Prioritize → Assign → Act → Verify → Close</span></div><div className="authority-note"><UserRound size={15} /><span>Actions are persisted locally for the demo. Connect this workflow to Firestore to synchronize assignments, SLA updates and resolution proof across authority accounts.</span></div></section>
      </main>
    </div>
  );
}
